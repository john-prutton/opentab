services:
  postgres:
    profiles:
      - dev
    image: postgres:18.2-alpine
    container_name: postgres_db
    restart: unless-stopped
    ports:
      - "${OPENTAB_POSTGRES_PORT}:5432"
    environment:
      POSTGRES_USER: ${OPENTAB_POSTGRES_USER}
      POSTGRES_PASSWORD: ${OPENTAB_POSTGRES_PASSWORD}
      POSTGRES_DB: ${OPENTAB_POSTGRES_DB}
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
